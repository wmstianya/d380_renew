# Tasks: 修复UART4主机回调函数签名

## 1. 修复回调签名

- [x] 1.1 修正 `uart4GetWriteData` 函数签名，增加 `uint16_t* regAddr` 参数
- [x] 1.2 修正函数内部数据偏移（从 `data[0]` 开始，而非 `data[7]`）
- [x] 1.3 移除函数内CRC计算（由 `modbus_master.c` 负责）
- [x] 1.4 设置正确的返回值（36字节纯数据，而非45字节含帧头）

## 2. 验证

- [x] 2.1 编译确认无警告
- [ ] 2.2 串口监控确认UART4发送查询帧
- [ ] 2.3 确认分机组状态能正常轮询采集

